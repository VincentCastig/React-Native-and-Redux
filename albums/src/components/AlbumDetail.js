import React from 'react';
import { View, Text } from 'react-native';

const AlbumList = () => {
    return (
        <View>
            <Text>text</Text>
        </View>
    );
};

export default AlbumList;